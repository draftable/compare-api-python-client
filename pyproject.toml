[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
target-version = ['py27', 'py36', 'py37', 'py38', 'py39'] # No option for 3.10 (yet)
